import type { ProjectFeatures } from './types.js';
/**
 * Detects project features to determine which plan outputs are applicable.
 */
export declare function detectFeatures(projectRoot: string): ProjectFeatures;

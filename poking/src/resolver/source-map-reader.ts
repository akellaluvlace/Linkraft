// Source map resolution is a V2 feature.
// This stub exists so imports are stable when source map support is added later.

export function resolveFromSourceMap(_element: HTMLElement): null {
  return null;
}
